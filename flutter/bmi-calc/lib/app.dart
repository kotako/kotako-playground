import 'package:flutter/material.dart';
import 'package:memorized/CalcScreen.dart';

class BMIApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) =>
      MaterialApp(title: 'BMI Calculator', home: CalcScreen());
}
